'use client'; import {useEffect,useState} from 'react'; import {DashboardShell} from '@/components/dashboard-shell';import {Loading} from '@/components/loading';import {PatientTable} from '@/components/patient-table';import {getPatients} from '@/lib/api/doctor';import type {PatientRow} from '@/lib/types';
export default function Patients(){const[q,setQ]=useState(''),[data,setData]=useState<PatientRow[]|null>(null);useEffect(()=>{setData(null);const id=setTimeout(()=>getPatients(q).then(setData).catch(()=>setData([])),250);return()=>clearTimeout(id)},[q]);return <DashboardShell><h1 className="text-2xl font-bold">Patients</h1><p className="mt-1 text-sm text-slate-500">Search patients by name or village.</p><input className="input mt-5 max-w-md" placeholder="Search patients…" value={q} onChange={e=>setQ(e.target.value)}/><div className="panel mt-4">{data?<PatientTable patients={data}/>:<Loading/>}</div></DashboardShell>}
