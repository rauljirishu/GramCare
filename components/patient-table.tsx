import Link from 'next/link'; import { RiskBadge } from '@/components/risk-badge'; import type { PatientRow } from '@/lib/types';
export function PatientTable({patients}:{patients:PatientRow[]}) {if(!patients.length)return <div className="py-12 text-center text-sm text-slate-500">No patients match this view.</div>;return <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b text-xs uppercase tracking-wide text-slate-500"><tr><th className="p-3">Patient</th><th className="p-3">Age</th><th className="p-3">Village</th><th className="p-3">Latest risk</th><th className="p-3"/></tr></thead><tbody>{patients.map(p=><tr key={p.id} className="border-b last:border-0 hover:bg-brand-50"><td className="p-3 font-semibold text-slate-800">{p.name}</td><td className="p-3">{p.age}</td><td className="p-3">{p.village||'—'}</td><td className="p-3"><RiskBadge level={p.latestRisk?.risk_level??null}/></td><td className="p-3 text-right"><Link className="font-medium text-brand-600" href={`/patients/${p.id}`}>Open</Link></td></tr>)}</tbody></table></div>}
